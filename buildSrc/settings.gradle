// This is required for the TOML file to read it form main catalog
dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
            // Point back to the main catalog in the root folder
            from(files("../gradle/libs.versions.toml"))
        }
    }
}
